version: 2
updates:
  # No package dependencies (only system frameworks), so the only thing to keep
  # current is the pinned GitHub Actions in the CI workflow.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
