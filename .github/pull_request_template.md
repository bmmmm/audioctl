## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Claims

Tick only what you actually did — these are checked against the diff, so an
unticked box is information, not a failure.

- [ ] `swift build -c release -Xswiftc -warnings-as-errors` is clean
- [ ] `swift test` passes
- [ ] I added a test for the pure-logic part of this change (or: it has none, explained below)
- [ ] I ran the affected commands against real audio hardware
- [ ] Docs updated (README / `--help` / `CONTRIBUTING.md`) if behaviour changed

## How I verified it

<!--
The commands you ran and what they printed — before and after where it matters.
For a device-specific fix, say which hardware you tested on; nobody else has it.
-->

## Anything reviewers should push back on

<!-- Trade-offs you made, things you were unsure about, or scope you left out. -->
